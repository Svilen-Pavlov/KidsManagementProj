@using KidsManagement.ViewModels.Students;

@model AllStudentsDetailsViewModel;
@{
    ViewData["Title"] = "Assign Children";
}

@*copy from listEligibleStudents (group)*@
<section class="py-5">
    <div class="container">
        <h1 style="margin: 2%; text-align: center;">Add chlidren from the list of Students</h1>
        <div>
            <div class="button-holder align-self-center">
                <a href="/Parents/Details?parentId=@this.ViewData["parentId"]" class="btn btn-info">Back to Parent Details</a>
            </div>
            <table class="table">
                <thead class="thead-dark" style="text-align: center">
                    <tr>
                        <th scope="col">Id</th>
                        <th scope="col">FullName</th>
                        <th scope="col">Gender</th>
                        <th scope="col">Age</th>
                        <th scope="col">Status</th>
                        <th scope="col">Choose Student</th>
                    </tr>
                </thead>
                <tbody style="text-align: center">
                    @foreach (var student in Model.Students.OrderBy(x => x.Id))
                    {
                        <tr>
                            <th scope="row">@student.Id</th>
                            <td>@student.FullName</td>
                            <td>@student.Gender</td>
                            <td>@student.Age</td>
                            <td>@student.Status</td>
                            <td>
                                <div class="button-holder">
                                    <a href="/Parents/AddStudents?studentId=@student.Id" class="btn btn-info">Assign Student as Child</a>
                                </div>
                            </td>
                        </tr>
                    }
                </tbody>

            </table>
        </div>
    </div>
</section>